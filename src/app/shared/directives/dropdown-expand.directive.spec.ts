import { DropdownExpandDirective } from './dropdown-expand.directive';

describe('DropdownExpandDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownExpandDirective();
    expect(directive).toBeTruthy();
  });
});
